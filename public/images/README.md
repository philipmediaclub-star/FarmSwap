Drop a real hero photo in this folder (e.g. hero-farm.jpg), then open
lib/site-config.ts and set:

  heroImage: "/images/hero-farm.jpg"

That's it — no other code changes needed. Leave it as null to keep the
illustrated placeholder background.
