/**
 * Site-wide content config.
 *
 * heroImage: set this to a real photo path once you have one licensed to use
 * (e.g. "/images/hero-farm.jpg" after adding the file to /public/images/).
 * Leave it null to keep the illustrated placeholder background.
 */
export const siteConfig = {
  heroImage:"/images/background.jpg",
};
